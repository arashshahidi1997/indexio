# Initialize A Config

## Write the default config

```bash
indexio init --root /path/to/project
```

This creates `.indexio/config.yaml` with sensible defaults.

## Custom output path

```bash
indexio init --output config/indexio.yaml
```

## Overwrite an existing config

```bash
indexio init --force
```
